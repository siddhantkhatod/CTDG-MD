services:
  ctdg-md:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      CTDG_CHECKPOINT: /models/ctdg_tvn_seed42.pt
      CTDG_DEVICE: cpu
    volumes:
      - ../outputs/checkpoints:/models:ro
    restart: unless-stopped
